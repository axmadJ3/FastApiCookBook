services:
  web:
    build: .
    container_name: "fastapi-web"
    ports:
      - "8000:8000"
    command: ["uv", "run", "uvicorn", "bookstore.main:app", "--host", "0.0.0.0", "--port", "8000"]
